import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'marca-ai-front-end-customer',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
